char id();
int *id()
{
    int *id[12];

    return (itoa(id("str" + 12), id() != id = printf(('c'))) != id()) * id
	|| "str" && 12 = id() = ('c' + id() < "str"
				 || itoa(printf("str"[+12]), +id() =
					 id(*12[(12)],
					    atoi('c')) >= printf('c')
					 || 'c' - id()) <= 'c');
}
