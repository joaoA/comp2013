int id;
char id()
{ {
} if ("str" = id())
    if (id
	(12 % ('c'),
	 id('c',
	    (itoa(id, 'c' - 12 != id())
	     [id
	      ('c' %
	       atoi(itoa("str", "str") <=
		    "str" + itoa(12 < id() = itoa(id, printf("str")) % 'c'
				 && !"str" = 'c' && id, id())) != atoi(id()
								       ||
								       itoa
								       (id
									(),
									id
									()
									==
									"str"))
	       || id)]))))
	while ('c');
    else
	while (atoi
	       (itoa
		(atoi
		 ('c' * 12["str" && &(id || id()) == id() > "str"['c']] =
		  id((12)
		     || id(), id * &itoa(!'c'
					 && id(id, id() < id(), 'c'),
					 12)['c'] = (itoa('c', 12)
						     || id()["str"]
						     && (12) >= itoa(12,
								     'c') >
						     12)) =
		  "str" % id("str", id(itoa(id() = "str" <= id, 12)) && !id
			     || 'c' || itoa("str" <= 12, +id())
			     || id() * itoa(12, id) = (12) % id
			     && id(itoa(((12)), id)) =
			     12 >= "str"[(id("str", id(id)) && -'c')] =
			     "str" || id() || 'c')), 'c' == 12
		 || 12 = "str")))
	    itoa(id()
		 || itoa(id(), (id = id("str", 'c')) =
			 id()[printf((id()[("str") || atoi(12)]))] != 'c'
			 || (12) =
			 "str" != 'c' < "str") * 12 - 'c'[id <= 12],
		 -itoa("str", "str" / id("str") || id()
		       || atoi(printf('c' <= id()[(id && 'c')]))) == "str"
		 && printf(*id() && id('c') >= 'c') = id || id || 12 < 12);
}

char id(char *id)
{
    int id[12], id;
}
