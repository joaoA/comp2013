int **id(char id)
{
    if (id() % 12
	|| id(itoa(id, id && id) = 'c') == id() == *12["str" = id
						       || id(!12, id(),
							     "str")[printf
								    ('c' =
								     id(*id
									()
									&&
									(itoa
									 (id,
									  id))
									-
									itoa
									('c',
									 id)
									==
									'c'
									==
									*
									(itoa
									 (id
									  [id
									   (12)]
									  ||
									  id
									  (),
									  id
									  (id,
									   printf
									   ("str")))),
									id)
								     || 'c'
								     ==
								     id
								     ())]]
	* (id));
    else if (id()
	     &&
	     atoi(id
		  (printf
		   (id
		    (id() -
		     12[(id()
			 && 'c' >
			 itoa(itoa
			      (itoa
			       (12, itoa('c', 'c') / 'c' = "str"
				|| 12 % atoi('c')), id)[12],
			      id()))]))) <= "str") >= 'c')
	while ('c' && 12);
}
