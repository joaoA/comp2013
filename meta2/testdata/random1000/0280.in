int id(int id)
{
    int id[12];
    char id[12], **id, *id;

    while (printf
	   (id() != 12['c'] < "str" || id - 12
	    && 'c' - itoa(id || +'c', id)[itoa("str", 12)] + 'c'))
	if (12 && "str" < id)
	    while (id)
		if (id()) {
		} else
		    return id("str"
			      && id / 'c' = atoi('c' = id(id, id(), id)) =
			      id() && printf(id()) > printf(id)
			      && "str") > id();
}
