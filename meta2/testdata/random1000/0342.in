int id();
int *id(char id);
char *id()
{
    return 12;
    if (id()
	['c'
	 || itoa(id, 12 / 'c' =
		 itoa('c', itoa(*12 = +id(id(), "str"), printf("str"))))
	 || (12 >= "str" = *"str" = 'c' || id)] + (itoa(id(id = 12), id())
						   || "str" !=
						   (atoi(12)) %
						   id('c'
						      [12
						       && "str" = "str"]) =
						   id[id && id < id()]))
	while (id)
	    if (atoi
		(itoa
		 (12,
		  &id(id = itoa('c', id) * printf('c') =
		      (printf(id))['c'])['c' / +(id) % id('c'
							  && "str" =
							  12,
							  (id()))[('c' <=
								   id())]]
		  || "str"
		  && (12 = id) =
		  "str" <= 'c' * (12) - 'c' * id() > (id)))[+id() !=
							    atoi(id
								 ('c',
								  12 *
								  atoi
								  ("str")))
							    ==
							    id['c'] *
							    printf('c')
							    &&
							    (itoa
							     ('c',
							      printf
							      ("str"))[12 =
								       id +
								       'c'])
							    <= id]);
	    else
		id()[12];
}
