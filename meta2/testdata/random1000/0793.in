char id(char *id, char id, char *id)
{
    char *id;
    int id[12], *id;

    while (itoa(((printf(printf('c') - 12[itoa(id, id())]))), !"str"[12])
	   || 'c' != printf('c') =
	   (-'c'[id(12) || itoa(printf(12), id())] =
	    "str"[itoa(id(), id('c', itoa(id(id), id("str") * id || id)))]
	    > 12 - id() = id()[12]
	    && atoi(-'c') * id()) == id("str" !=
					id()[(atoi
					      ((printf("str") <=
						-itoa('c', 12
						      || 12))) - itoa(id,
								      printf
								      ("str"
								       =
								       "str"
								       >
								       id)
								      ||
								      12) +
					      'c')] || ((12)[id = 12]))) {
    }
}

int **id()
{
    char *id, *id;

    return id() = (!id =
		   itoa(id("str"), 'c' * id[atoi(12)]
			|| "str" > 'c')) * "str";
}
