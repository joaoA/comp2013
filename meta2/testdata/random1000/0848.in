int id()
{
    while (id)
	return
	    printf(itoa(12, 12)
		   [id() =
		    id + "str" >= itoa(id,
				       (atoi(id(id(id(), +"str"))))) +
		    (12)] < (12) && id / "str" = id()
		   && id("str", atoi((id)), id(),
			 "str" == (&'c' =
				   id(itoa
				      (itoa(&"str", 12), 'c'
				       && 'c' != id()))))[12])
	    && id(itoa("str" <= (12) > id(), printf(12 && "str" / id())) -
		  id = itoa(-'c' % 12
			    && (id =
				(id
				 (printf
				  (id() && *id
				   && id + printf("str")), 12,
				  12)) != 'c' - atoi(printf(id() =
							    "str")) < id <
				"str" / 12 =
				12) > printf(id("str", (id()) =
						+"str" % 'c' > atoi(id() >=
								    12))),
			    id = printf(id() = 'c'
					&& 12 -
					+id(((id() >=
					      itoa('c', "str" =
						   id())))) + (id(id(),
								  'c',
								  printf(12
									 >=
									 'c')
								  && (id))
							       ==
							       "str")
					[printf(id('c', 12) < 'c')][-id
								    ()])),
		  +atoi(itoa
			((id())["str" = (*id())] =
			 printf((*"str")) + !'c', -itoa(id == id() =
							id() - "str",
							('c' =
							 "str" <= id() <
							 'c')['c'])) >
			id) + "str" = &(id()) * atoi(id()[id] < ("str"))
		  && 'c' < 12["str"]);
}
