char *id(int * *id, char *id, int id);
int *id()
{
    char *id[12];
    char id;

    if (12) {
	while (12)
	    if (id())
		'c' / 12 * id() && ("str"[id()][12]) = id()
		    || itoa("str" != "str", printf(id)
			    || printf(id + id()) == atoi(id()))[id()];
    } else;
}
