int *id()
{
    char id;
    char **id[12];

    if (itoa
	(12 * !id ==
	 itoa(atoi
	      (12 >
	       id(id(id() || id - id() && "str", printf(id))) % "str" -
	       (id())), (12["str"] = atoi(!(id())) != id
			 || atoi(12)[id()]) * id), ('c' || (id[id()]))))
	if (id('c'))
	    "str" && !id;
	else {
	}
}
