int id()
{
    int id[12], id[12];

    while (itoa(atoi(-(12)), id()) = -"str"
	   ||
	   ((id() -
	     itoa(id()["str"] / printf("str"),
		  id(printf(id()) * id()[12 * id] = ("str"))
		  || id(atoi('c')[atoi("str")], id()))) = 12 >= "str"));
}
