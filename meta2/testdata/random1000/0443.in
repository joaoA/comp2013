char **id()
{
    return 'c' % id && -!'c' * id
	&& atoi("str" = (*id()) = itoa(12 >= id(), id())) > 'c'
	|| id() != !id[id("str", printf(12), atoi('c' && id)[12]) <=
		       "str"] = id();
}

int id, id[12];
