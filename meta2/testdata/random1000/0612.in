char id(int id);
int *id(char * *id, char *id)
{
    return 12 % itoa(id(), atoi(id())) >= +'c' =
	id("str", atoi(id) < 'c')[printf(itoa(!id(), "str") = 'c')
				  || atoi("str") / id || 12] >= "str"[-'c'
								      ||
								      id
								      (id)];
}
