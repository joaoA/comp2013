char id[12];
char *id[12];
int *id(char *id, int id);
int id()
{
    if (!itoa
	(12 - 12
	 || id - itoa("str" >= 12
		      || -id(id !=
			     (id)[id(id, 12, atoi(id() - 'c')) <= 12] =
			     "str") = id(id, id() =
					 id((id)) <= "str"),
		      (printf(id >= "str"))),
	 id())[id(printf(atoi("str") < id) > "str", 12,
		  id((!itoa(printf(itoa(id, id()) * id()) = "str", 12)) -
		     id) > itoa(+("str" = id
				  && atoi(id))[id()],
				"str")[itoa(+id() - 12 =
					    id(), ('c') % id)] =
		  (!'c' / &printf('c')
		   && id - ("str" <
			    ('c' - id() =
			     atoi(itoa("str", itoa('c', 'c' <= 'c')))))))
	       && id / atoi(id)[id()] = 12['c'] * printf(12) != "str"])
	return -(id() - id(id) <
		 printf(itoa
			('c' / id() || 'c' && "str", "str"
			 && 'c' = id("str") || id() == "str")[&12
							      || id()]) +
		 'c') > atoi(itoa(id(id, id = id) == id(),
				  id((id())
				     || id("str",
					   itoa('c', "str"
						|| id(id('c') >=
						      ((12 == 12) * 'c') =
						      12)[('c')]),
					   itoa("str"[!id], 'c'),
					   "str") > 12))
			     && itoa('c',
				     id((id) =
					atoi(id()
					     || 12 * id), 12) * "str" =
				     12[id()]) != id(12) =
			     id(12, id())) % 'c';
    else
	while (*'c' - id(id(), &(id)) && id
	       && "str" != "str" = id() =
	       id(id) > 12 - itoa(-12 > id(id, 12, "str"), id
				  || 'c' >= printf(id)) * "str" || id())
	    while (itoa
		   (12, *id
		    && 12[!(12 % id(id(), 'c') != 12 < id() * 12) && 12
			  || id][12 + *"str" && 12 + 'c']));
}
