int id()
{
    if ((id) + ("str") && 12
	|| id(id()) < 'c' = itoa("str" =
				 (*'c') == id("str"[id()]["str"], id())
				 || (id()) > ('c') / id
				 || 12, printf(id("str"))))
	return atoi(-'c' * 'c')
	    || printf('c') + "str" >=
	    id(printf
	       (id()
		&& id = !id('c', 'c', atoi(12) + id
			    || itoa(-12 = id, 'c') + 'c' % id(id()), id
			    && itoa('c', id()))));
    else;
}
