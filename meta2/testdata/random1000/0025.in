int *id[12], *id;
int **id(int *id)
{
    return
	&printf(("str" ==
		 itoa(id
		      (printf(12)
		       && (id *
			   itoa(id(!id[printf("str")], atoi("str"), id())
				>= (id(12, 'c', "str")) = "str", id())
			   || printf(id["str"])) <= id - "str", !id), id =
		      itoa(id, 'c') % id[12] > 'c'['c' % id = "str"]
		      &&
		      id(atoi
			 (-itoa(id, "str" = "str" - printf(id) == 12['c']))
			 = id()[id()] < 'c'
			 && 'c' > id(id, 12, id) * id() > 'c'
			 && 12)))[printf('c' && "str"
					 && id(id < id, id() =
					       id()) / 'c' > id() =
					 id != +"str" && "str" + id)]);;
}
