char id(int * *id)
{
    int *id[12], **id;
    char id[12], *id[12];

    return id() <= *"str" / &id(*atoi(12 <= id), 12 =
				id(), "str")[id((printf('c')),
						"str" <= 12 =
						itoa(itoa("str", 'c'), 'c')
						&& 'c'
						|| id((printf(id()))))]
	&& (12) * 'c'[id];
}

int id, **id[12];
int id(char *id, char *id);
