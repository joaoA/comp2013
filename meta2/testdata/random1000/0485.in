char id()
{
    int id, *id;

    if (('c') || +"str" <= 'c'[id()] = "str")
	if (12)
	    while (id('c', "str")) {;
	} else
	    while (itoa
		   (atoi
		    (id
		     (12, id() =
		      ("str" = 'c'
		       || id() != 'c' * id()) + 12 > id() = id()
		      && 12 * itoa(id(),
				   12)[id()[&("str")
					    || id = 12 =
					    12 - id()]] - 'c')), ('c') =
		    id())) {
	    }
}

int id(int id, char *id);
