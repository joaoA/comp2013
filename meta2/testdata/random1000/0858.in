char id()
{
    char *id, **id;
    int **id[12], id;
    char *id;

    if ('c' -
	printf(itoa(12 = (id() <= id), id(('c')))
	       [-
		(itoa("str", (id(printf(atoi(id))) = "str")) =
		 id(id(), "str", 'c') || ("str")) < itoa(id, "str" + 'c')])
	&& 'c'['c'] - id() >= id('c', id())) {
    } else;
    if (id() = printf(12) || itoa(printf(id()), id()) < 12)
	if (12 / "str" ==
	    itoa(id("str" = "str"), id(printf(-"str")) =
		 id / id("str")) - "str"[id(itoa(id('c'), "str"),
					    itoa(("str" = 12
						  || id(-'c',
							id ==
							+"str" /
							(printf
							 (+id <= &id())
							 || +"str")
							|| itoa("str",
								id(12)) <
							('c') / 12 =
							printf(id()))),
						 'c') - "str")] > (id()
								   && 12 -
								   itoa
								   ('c',
								    "str")
								   ==
								   id(id
								      [id]))
	    [id < 12 = printf(&'c') == &itoa(12 % "str", id()) / 12 || id])
	    while ('c' > -12 % printf('c') > 12 - printf(12) =
		   printf(id || itoa(12, "str") % printf(12) = id('c')
			  && itoa(itoa(12, id()), 'c') = id) && id());
	else {
	    while ('c')
		id();
    } else
	return id();
}
