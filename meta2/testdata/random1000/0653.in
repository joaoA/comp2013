char id(char * * *id)
{ { {
}} if (id
       (id,
	(itoa
	 (printf(id('c') != +id() > 12 || "str"), "str" > 'c'
	  || 12 > id) / itoa(-"str"
			     && &*id() < "str" % id, "str")[id]["str"]) =
	12 * itoa(("str" > itoa(id >= &id(), 12)), id) + itoa(id(), 'c'),
	!id));
}

int id(int id)
{
}
