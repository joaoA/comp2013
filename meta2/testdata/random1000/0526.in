char id(char id)
{
    if (itoa
	(id(),
	 (12[id() >= id()] *
	  printf(itoa
		 ("str" =
		  "str"[id(12, id) <=
			id((id) = id('c', 12)
			   && printf(id
				     && id()[-"str"]), itoa('c',
							    "str" <
							    id()))] >=
		  "str" || 12
		  &&
		  -*"str"[id
			  (atoi(id(id()))["str"], 12
			   && printf("str"
				     && id(12, id() == "str") + 12))],
		  'c'[id() * 12]) <= itoa("str", 'c'))))) {
    } else
	while ("str" / "str" = id() && atoi('c') && printf('c' % id()));
}

int id;
