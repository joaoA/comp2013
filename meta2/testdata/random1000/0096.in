int *id(char id);
int *id()
{ {
} return -12 && 12;
if (itoa
    ((id("str" = id() > &id, id) >= "str" >= id() = id % 12
      ||
      itoa(itoa
	   (id(12),
	    atoi(id
		 ((id), id
		  || 'c' - atoi("str" !=
				"str"[&'c'])[printf("str")] - "str",
		  'c'))), 'c') = id || &atoi(12["str" - 'c']) >= atoi(id()
								      &&
								      atoi
								      ('c')
								      *
								      12) =
      &itoa(printf
	    (id(id()) /
	     (id(id(id), 12, "str" == 12 == id)['c'] <=
	      id(itoa(id, 'c') != id()[id] % id()
		 && 'c'[id()], itoa(!id, "str")[12]) = 12) =
	     id("str")[12] >= 'c'), atoi(id()) == 12) - 12
      && 12 >= id(itoa('c', "str")["str"], id,
		  *itoa(itoa
			("str",
			 id(id =
			    atoi("str") < id(), 'c'[("str")], "str",
			    "str")), 12)[id] + id(!"str"))
      || id()),
     printf(id
	    ((id[12 || 'c'][+id("str"[id], 12 || id()) && 12 * id]) +
	     id)) > id(!'c')))
    !'c' > 'c' - id && id;
else
    id((-id()) > id,
       itoa(printf
	    (printf
	     (id >= "str" < ("str" * 'c' * ('c')) =
	      id()['c' * 'c'] / !id()["str" || ("str")
				      && 12] / 'c')),
	    "str" * id / (atoi("str") !=
			  atoi('c'
			       ['c'
				[id - atoi(itoa("str", 'c')) = 'c'
				 || id() >= 'c']])))) =
id <= (12 != 12 <
 -id[+itoa(("str"), itoa(id(), -id(!(atoi('c'))))) + printf("str") -
     id(itoa(printf(itoa(itoa(12, 12 == id() = 'c'), &id(id, id()))), 'c'),
	itoa(-atoi(12) > "str", 12))
     && id() * itoa(itoa("str", id), printf(id)) != 12
     || itoa(id, atoi(id() + id() * 12)) && itoa("str", "str") / &12]);
}
