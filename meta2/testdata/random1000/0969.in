int id(int id, int id)
{
    char *id[12];

    id(id
       ("str" < 'c'["str"]
	|| id - -"str" =
	12 * id >= printf("str"),
	id() + *12 <= itoa((id)["str" = 'c' && id()],
			   (12 * atoi(itoa("str" = id, 'c')) != 'c'[id()]
			    && 12)) != itoa(id, (id || 12
						 || 'c')),
	itoa((printf(id)["str"]), atoi(12) || id % 'c' && id()) < *12
	|| atoi(id) / id / -"str")) <= itoa(('c'), 'c'); {
	if ("str"
	    || id(!id && "str"
		  || id() =
		  itoa(itoa
		       (id,
			(id
			 ('c' = 12, printf('c') =
			  'c' >= 'c' != (12), -printf(12 % 'c') =
			  atoi(12), "str") / 12 && 12)), "str"
		       || id()) != id = printf(12)) == id + -id < id < id =
	    12 % itoa(id, id("str"))["str"] >= id[(itoa(id, 12)) / id])
	    if (((12 == "str") / -printf(id)))
		return atoi(12 && id) && id;
	    else
		'c';
    }
}
