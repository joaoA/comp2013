char **id[12], id;
int *id(char *id)
{
    int id[12];
    int id[12];

    if ((atoi(id() <= 12[(id(id || id(), id > id))] / 'c' && id(id()))
	 [id(id() && itoa(id[12], id)) + id()]))
	return id;
    else;;
}
