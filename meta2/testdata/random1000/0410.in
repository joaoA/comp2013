char id(int *id)
{
    int **id, id; {
	return itoa("str" < "str"[+atoi(atoi(id))] =
		    id() / id < *('c')['c'] =
		    (id('c' > !"str" * "str" && *'c', 12)) +
		    'c'[id
			(id = id(), 12
			 || printf((id())
				   || &atoi(id) =
				   id(id()) <= (id)[itoa(12, 'c')])) + id <
			"str" % id('c')]
		    || itoa('c',
			    (*id(id['c' > id()]) = id('c') / !12[id()] =
			     "str"
			     && id() < "str")), "str" + &id(&id =
							    id(atoi('c'))
							    && "str") <=
		    printf((id()) != id()) >= id);
    }
}

int id(int id)
{
    return *id;
}
