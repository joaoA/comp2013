char *id()
{
    char *id;
    int id[12];

    if (id(12, ('c' < !"str" = printf('c'))) * id(id, id) > id - +'c' =
	id((id)[12 && id], (id('c' || 'c', id))) % atoi("str" >= 12
							&&
							&id(id
							    ((id), 'c'
							     ||
							     12[(id()
								 ||
								 itoa(id,
								      12))]
							     && id(),
							     id()),
							    12) >=
							"str" -
							atoi("str") *
							id() < !id() =
							(itoa
							 (id(),
							  itoa(12,
							       12))['c' >
								    id])
							|| 12))
	if ((id() && &id))
	    return 12;
	else if (&12)
	    if (atoi(12))
		!id() && "str" / (printf('c'))
		    && id() > id(+(12 - id()[id()])) || 12 && id;
	    else
		return (!"str"[12 || 12] /
			!itoa(id !=
			      (id
			       (itoa
				(printf('c'), "str" || printf(printf(id))
				 || 12 = id)) = id(id(id)
						   &&
						   printf(-itoa
							  ('c' > 12 <=
							   id(),
							   itoa(printf
								('c'),
								12) =
							   "str" > 12 =
							   printf('c'))))),
			      id >= id()) / id()
			|| id()[12 ==
				'c'][-itoa("str",
					   id(printf(id())
					      && "str" = id, id)) && id
				     || id() = "str" >= itoa('c', 'c'
							     &&
							     atoi(id
								  (+id(),
								   id(id()
								      =
								      itoa
								      (id
								       (12),
								       'c'),
								      id >
								      id())
								   &&
								   id(id
								      ()),
								   id)))]);
}

int id[12];
