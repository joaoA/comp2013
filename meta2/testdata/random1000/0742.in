int id()
{
    char id[12];
    char *id[12], *id;

    itoa(itoa(*"str", printf(("str"))) % 'c' <= ('c') =
	 12, id(id(), 12 / -id()[id]
		&& (printf(12 > 12) =
		    printf(id())), atoi(printf(itoa('c', 12) =
					       12["str"]) >= 12 < ("str" =
								   id()
								   &&
								   id()[id
									>
									&12
									&&
									atoi
									(id
									 ())]))))
	+ id && *"str" = 'c'["str"] || *&'c'
	|| printf(12) >= itoa('c',
			      &id)[id(printf
				      (id() = *"str"
				       && id >= (id() >
						 'c' * 'c')))] *
	id((id()) =
	   12 % atoi(id()), itoa("str" / &"str"[id() <= ((printf(id())))],
				 +atoi(id())), printf(+('c' || 12)));
}
