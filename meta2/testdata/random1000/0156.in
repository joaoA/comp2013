int *id()
{
    if (itoa
	(*12[12 = 'c' || *id() < id + id() && id() < (id)], "str"
	 || "str" > 12[12]))
	("str" >
	 'c') + atoi(id(12
			&& id() > "str" > "str" = id(atoi('c') = 'c'
						     && &"str")[id((id()) <
								   id < id,
								   *'c')
								[itoa
								 ("str" =
								  *id() ==
								  id(),
								  +id() <
								  "str" =
								  id * id()
								  || id %
								  'c' >
								  id = id()
								  || "str"
								  >
								  id()
								  ['c'])]],
			id = 'c'
			|| (('c' || 12 == "str" > 'c'[&atoi('c')])) <
			itoa('c', 12) = id, ("str") =
			id <= (id[+12]) >
			itoa(id
			     (id(("str")) < id(), *"str"
			      && "str" = ('c' = id())), id()[id()] =
			     id()) * id(+"str" % "str" == id)[id()
							      || "str"]))
    &&
    *atoi(itoa
	  ((itoa
	    ("str",
	     id((12 < id
		 || "str" =
		 id(id()[id]) ==
		 id('c')[id
			 (id =
			  12["str"][-"str"] != 12, 'c' >= 'c'['c'], 'c')]
		 && atoi(12)), itoa((id()), 12 =
				    id <= id())) / id)), id() = 12 % 12 =
	   id <= "str"));
    while (12 * 12 - 12);
}

char *id[12], *id;
