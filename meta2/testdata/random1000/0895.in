char *id(char *id)
{
    char id;

    char id[12], id; {
	if (id() <=
	    itoa(id
		 ["str"
		  || id("str" = id() && id + id >= 'c' = id
			|| 12, id - (id), id()) >= id()]
		 || "str", id(id) = 12));
	else
	    +id(itoa((-12), +12 * 'c'),
		printf(id(id)
		       || 'c' =
		       itoa(id(),
			    +id())[itoa(id(), 'c') % itoa(id() =
							  *(itoa(id, 12)),
							  12) - id() =
				   id - 12 <= itoa(!12, id('c'))
				   || (itoa("str", 'c'))]), itoa('c'
								 &&
								 atoi(id
								      ('c'))
								 %
								 itoa
								 (printf
								  (id())
								  && (id),
								  id)['c' -
								      id
								      ("str")],
								 'c')) !=
		id(atoi
		   ((printf(id() % itoa("str", 'c' - id()))) % 'c'
		    || itoa(id(),
			    *(id('c' * 'c', id(12, id()), 'c')))[itoa(id
								      (12,
								       id *
								       printf
								       ('c'
									<=
									printf
									("str"
									 ||
									 id))
								       =
								       id %
								       +12
								       &&
								       itoa
								       ("str",
									printf
									("str")),
								       "str"),
								      id
								      ())]))
		<= atoi(id()) && 'c' = "str";
    }
}
