char *id()
{
    if ("str")
	while (id < !id(id));
    else
	id()[id() && id() >= (*12) || -"str"["str"]];
    while (id
	   (id() = "str"
	    ||
	    itoa(itoa
		 ((id
		   (id =
		    'c' - atoi(12)[printf(12 && "str") >= "str"],
		    12 % atoi(id()) =
		    id() == "str")), atoi(id)) > "str", 'c') / 12 > id,
	    itoa("str", id) || 'c')[id])
	while (12 -
	       id((12), 12, id("str") != (12) * id
		  && "str"["str" = ("str") && id() || 'c']))
	    "str";
}
