char id(int * * * *id, int * *id, char *id)
{
    int id[12];

    if ('c' || id() > itoa(*('c') - id, id(id(atoi(12), -12) + 'c' = id()))
	&&
	printf(itoa
	       (&id = "str" == "str" = itoa("str", 12 - id), id()
		&& +id()))) {
    } else
	printf(12);
}

int id()
{
    char id, id;
}
