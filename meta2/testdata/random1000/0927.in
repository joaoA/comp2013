int id(char *id)
{
    int id; {
	return atoi('c')[12] * id
	    || "str" %
	    id(id(id(), itoa(12, id) > printf(&(itoa("str", "str"))))) =
	    itoa(itoa(id(), id()), 12) > id() + !12[(id)]
	    || 'c' >= id[('c' = +12)];
    };
}
