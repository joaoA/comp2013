char id();
int id()
{
    if (12 || "str"
	&& id() / itoa('c',
		       12[12 % atoi((("str") = 12))
			  || &(id('c', "str")) = "str"]) > id < ("str"))
	return atoi(id());
    else;
    while ("str")
	if ("str" /
	    (*id
	     (id || 'c' > id() + printf("str")
	      || atoi('c'), 12 >= "str" < printf(atoi(id)))) = id()[id])
	    ('c' = &atoi(id())
	     && &"str" % "str"['c' %
			       itoa(itoa
				    (12
				     && 'c' / id()[id('c')] = "str" =
				     "str" - -"str", *id)
				    || 12 > "str", 'c')]) < id > itoa(12,
								      id()
								      !=
								      id()
								      +
								      12[id
									 (+id
									  [('c') * 'c' && id(-'c', 'c')] + "str" && (id()), id())] <= id = id);
	else {
	}
}

int id();
