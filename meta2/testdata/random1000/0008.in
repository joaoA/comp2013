int **id(int *id)
{
    int id;
} int id();
char *id()
{
    return 12 <=
	atoi(id('c')) *
	id(id()
	   [atoi(+itoa("str" = 12, itoa('c', printf(id() || 12)) = id())) *
	    id() != id(12) = id]
	   || 12,
	   +printf('c'))[printf(("str" <=
				 itoa('c' =
				      *id(id, "str",
					  itoa(id("str", (id)), 12)) =
				      atoi(id("str"[id])) ==
				      id(atoi
					 (+'c' *
					  +itoa('c' >= 'c'
						|| "str", 'c')))[12 != -id]
				      || 'c', printf(id() = id)) + ('c')
				 || id((12 % id || "str")))) *
			 12[id
			    (itoa("str", "str") <=
			     printf(atoi(itoa("str", ("str")))),
			     'c' > itoa(id, ('c'))) && id]];
}
