char id(int *id, char *id)
{ {
}} int id()
{
    char id[12];

    "str" =
	(id
	 (id
	  (12,
	   &itoa(id || 12['c' || "str" >= id() / 'c' = "str"], id
		 || printf(id))) % id = 'c'
	  && printf(id()) / (12) =
	  itoa(itoa
	       (printf(-'c' % itoa(+id() = id, 'c') != id()),
		(id()) % "str"), id) == atoi(-id()
					     || id(id <
						   itoa('c' >= id = 12[id]
							|| 'c' % printf(id)
							|| "str", id),
						   id())), id)['c']);
}
