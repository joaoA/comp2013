int id()
{
    if (id
	(itoa(+12, 'c' / -'c' = id()), 'c', printf(id('c', id()) != id) =
	 itoa((12), id(*"str" && printf("str") - *'c' - 12) > 'c'
	      ||
	      printf(id
		     ("str",
		      -*(id) - "str" == "str"["str"] / itoa("str",
							    id))) *
	      printf(atoi(12)) && 12) = *"str" || "str" - id())) {
    } else {
    }
}
