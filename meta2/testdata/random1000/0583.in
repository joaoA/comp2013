char id[12], *id[12];
char *id()
{
    if (("str" *
	 id(itoa(*"str"[itoa(printf(id()), 'c') = 'c'] != id[12], 'c'),
	    (id("str")), id)) - 'c') {
    } else if (-'c'
	       &&
	       (printf
		(+12[id] =
		 12[id(id() / "str"[printf(id) != atoi("str") && id()])] !=
		 12 * "str" && 12 * atoi("str"))));
    else
	'c';
    while (itoa
	   (-id(id()[id(('c') = +'c' % id <= atoi(12[id])) && 'c']),
	    -(id()) * id(id) && id))
	if (itoa
	    (atoi
	     (id =
	      (!itoa(id(), 'c')[id("str") && 12] == (id()) + &12 == id()
	       && id(id,
		     (-12 >= id()["str" = id()] * &"str")[id(!id())] <
		     id() * "str" && itoa("str", 'c')) / "str" || "str"
	       && id != 12 = -id() + 'c' <= id())), id))
	    12;
	else
	    id('c') < 'c' || id() || "str" - itoa('c', atoi(id)
						  || 'c')[printf((id) <=
								 ('c') -
								 id)
							  && +id < (id() =
								    +12)];
}
