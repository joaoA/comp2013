int id(char id)
{
    int *id;
} char *id()
{
    if ((id
	 (printf("str")
	  || (id())[12 / 12] == ("str") - atoi(id() =
					       id !=
					       (printf
						(itoa(id(), 'c'))))))) {
    } else
	while (id)
	    id(12 <= id == id() / "str"[12 > id])['c'] || 12
		|| "str" = 12 + "str"
		|| ('c') = itoa(id = (id()) =
				"str", "str")[*'c'] < id["str" || id];
}
