int id()
{
    return itoa('c' < id() =
		atoi(printf(id())["str"] <= &id(12, 'c', id) > "str"
		     && 'c') - atoi(id(12 =
				       id))[id() *
					    ('c')[!id
						  (itoa
						   (id(),
						    id('c'
						       && 'c')))] ==
					    atoi("str")],
		+atoi("str") / id) < 'c' && id
	&&
	12[itoa
	   (atoi(12 = id(itoa((- -12), id || 12)) && id()),
	    printf(12 || id()))] - ("str");
    if ("str")
	if (id()) {
	} else;
    (id()) <= itoa(id(),
		   id(printf(id()) >
		      'c'["str" || 'c'
			  && (id(id) / 12 || id() < "str") > 'c']
		      || &'c' * *12[printf(12)][id()] / id(id(), printf(12)
							   || !printf(id) /
							   atoi(id) >= id >
							   "str" + 'c' =
							   "str" * 'c'
							   || 12 =
							   printf('c'),
							   'c')));
}

char ***id()
{
    while (id('c') <=
	   (itoa
	    (12
	     || *id(id <= 'c' * itoa(id(), id) =
		    'c'["str"], (id)), !itoa("str" / atoi("str"), id =
					     atoi(-'c'[id(12, id(), id())])
					     || 12)) || atoi('c'))
	   && "str" * id()[id()] && 12 = 'c') {
    }
    return printf('c');
}
