int id()
{
    char id;
    id *id(atoi("str" < "str" - 'c')[*id] * 12
	   || 'c' < &id, *printf(atoi(id) - id
				 ||
				 id(((itoa
				      (!id(), 12
				       || atoi(id('c') = 'c' = id())[12 =
								     'c' /
								     12]))))
				 > 12[&id()]
				 && 'c'[id(12)] - 12 % 12),
	   "str" % 'c' != -id || printf(12) = "str");
}
