char id(char id)
{
    char id[12], *id[12];
    char *id[12];

    return 12 % 'c' =
	printf('c'
	       [id
		("str", "str" >= (12 <= id[id('c') = id] || &id)
		 && "str" - itoa(id,
				 id)[itoa(itoa
					  (id(&"str"),
					   id(printf(12)
					      || printf(id())) =
					   12) + atoi('c' % (id(12, 'c'))),
					  "str") != id * 12] < id(id())
		 || id(id()) % 12 * 'c', 12)]) = 12 >= 12
	&& *id("str"[id()],
	       "str" > ('c' || "str") / id() <= 12) % (-12 / "str"[id]);
}
