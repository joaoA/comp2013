char *id(int *id)
{
    char id;

    while ((printf(12 > "str" || id() <= id(12)) + id) * id() = id())
	if ("str" / printf(id()))
	    return +id()[id()];
}

int id()
{
    int id[12], id[12];
} int id()
{
    char ***id[12], id[12];

    while (12)
	if ('c' - (id) = id('c', 12)) {
	}
}
