int id(char * *id)
{
    int id[12];
    int *id;

    if ("str")
	while (itoa('c', (printf(&id()))) =
	       printf(itoa
		      (("str" / id(id(id(), atoi('c') != atoi('c')))[12] ==
			id()["str"] || "str"[id])["str"] || printf('c'
								   &&
								   "str"[('c') * !id("str", itoa(-printf("str") && id < 12['c'], "str"), id) >= id() > ((12)) = id / itoa(id, 'c') = id && !(12)[id(id())]]) > ("str"), 12 != id() || (atoi("str")))))
	    12 < itoa('c'[12] || id, id(id() || "str")[+12 / ("str" + 'c')]
		      && "str");
}
