int id();
int id()
{
} char **id()
{
    if ((id((id))
	 || 'c' == atoi(&"str"
			|| id(itoa("str" - 'c', id()) <=
			      itoa(12
				   && "str" % atoi("str"), id =
				   id() >= 'c' = 12 && 'c'["str"])))));
}
