int id()
{
    int *id[12], **id[12];

    if (id() +
	"str"[itoa
	      (atoi
	       ((id(id || 12 || id()) = printf("str")) % atoi(id()) && 'c'
		&& "str"
		&& 12), 12)["str"]] <= printf(id(12)) / (itoa("str" * id,
							      'c')) =
	(id()) || ("str" * 12 = id) || 'c')
	id && id() >= id['c' > id(12, id())];
}

int id(int id);
char *id(char *id, char id);
