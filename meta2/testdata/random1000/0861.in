char id(char id)
{
    !itoa(12, id() = id(12) != &id()
	  ||
	  !"str"[atoi
		 (id
		  (id
		   ('c' % 'c', &"str"["str"] <= 12 < 12,
		    id()[id] >= 12["str"], 'c' != id))) =
		 ("str" >
		  printf(itoa(id(id, "str", 12) == id(), "str")["str"]))
		 && id()] % 'c'[id()] - atoi('c' < id()));
} char *id()
{
    char *id[12];
}
