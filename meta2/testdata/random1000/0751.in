char **id[12], id[12];
char id(int *id, char * *id, char * * *id)
{
    if (!(id())
	[12 != printf(itoa(id(), id()) = (12)) % (id(id())) >
	 itoa(+('c'), 12 / itoa(id(12 / 12), 12)) = id() > (itoa(id, 12))
	 || 'c' % &atoi("str" =
			id("str",
			   'c'[itoa(atoi('c') == (12) = id * 12, 'c')])
			&& id)] && 'c')
	if ('c')
	    while (itoa('c', id()) =
		   *-id(id()
			|| 12, 'c',
			("str")) + 12 / id(id[+'c' >
					      itoa(atoi
						   ('c'[id()] +
						    atoi("str")),
						   printf("str"))
					      && (12)] =
					   (id)[atoi('c')], 'c') && "str"
		   && 12 <= 'c'[(atoi('c')) / "str"]);
	else;
    if (-'c' =
	itoa(id() * 12 || "str"[printf(id()) || id] <= 12 = 12 + id, id))
	return "str";
}

int id();
