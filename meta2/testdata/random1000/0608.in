int *id()
{
    int **id, *id;

    if (id("str" - id() && 'c') > 12 <= !12 =
	12[(itoa
	    (+"str" && 'c'
	     || id(+"str" != id() > atoi(id)
		   && id() * "str", ("str")) * id(id()) - 12
	     || id(), atoi(12 * 'c' - (12))))])
	if (12 == 'c')
	    while (id(printf(12)))
		while (12[*"str" || "str" = id() == 'c'])
		    if ("str")
			return &12;
		    else if (id
			     (printf(12 - *'c')
			      && itoa(*12[printf((&+id / 12) = +'c')],
				      'c'))[12 + 'c' = 'c'
					    && (atoi(*'c') % id)])
			return 12 / id();
		    else if (&printf
			     (itoa
			      (id,
			       itoa(id < (12) =
				    id(("str")["str" && 12] > id()),
				    (id())) != 12 != id())) || id != id()
			     || id("str" =
				   itoa(+"str" = id >= 'c' > id
					|| !printf("str"),
					'c')) / printf(12) =
			     itoa(itoa
				  (id
				   (printf
				    (-'c' = id = 'c' % (id())
				     || id(12 -
					   ('c'
					    &&
					    id()[atoi
						 ((atoi
						   (id > id() < "str" = 12
						    || "str"
						    && id())))])))),
				   atoi(12
					&& 12)),
				  -id() != printf(12 <=
						  'c') +
				  12)[itoa((atoi(12 && id((id()), 'c'))) =
					   ("str") || 'c'
					   && id() + 12 * (atoi('c')) ==
					   12 +
					   printf(id
						  (atoi
						   ((id()) > 12 + -'c'
						    && id() + id
						    && 'c'), (id == id =
							      'c')))[id] <
					   (12 =
					    itoa(12,
						 id() + "str") <= "str")
					   && id(id), id(id()) + id
					   && *("str")
					   ||
					   id(id
					      (itoa('c', 'c'),
					       "str" !=
					       -"str") * (12 % id() >= 12),
					      'c') = id(id > (12)
							|| printf((id) >
								  'c')[atoi
								       (itoa
									((12), 'c')) < "str"]) - "str")]);
}

int id();
