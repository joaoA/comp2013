char *id()
{
    if (itoa((12), id))
	if (itoa
	    (itoa(id(), id() - id() / id = printf(id("str"))) || atoi('c')
	     || id() > ('c'),
	     id[atoi
		('c'
		 || id(12 <= printf("str" > id * -id()) * 12,
		       ('c') <= id() == 12) = id()[12 || !'c' >= id] + id()
		 || "str" > id((itoa(id, id() = id[id]))[-id != 12], id,
			       id()) == 'c' / atoi(*12[id()
						       && (id(12) !=
							   'c')])) =
		id()]))
	    'c' != id() >= 12;
	else
	    'c' * 'c' != itoa(printf("str" = 12), ('c'[id]) * &12 && !12)
		|| id() <= id;
}

int id(int * *id, char *id, char id, int *id);
