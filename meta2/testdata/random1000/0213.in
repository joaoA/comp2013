int id(char id, char *id)
{
    char id[12];
    int id[12], id, **id;

    return
	printf(id
	       (-12 > id() % 'c' =
		((id)) < 'c' / itoa("str", id()) == id(12,
						       &(itoa
							 ((atoi('c')), 12))
						       && "str")[id()
								 && 'c'] -
		itoa(id("str") == "str", id) + 12,
		id(+itoa(-"str", id() == 12))) || -(+12));
}

char **id, *id[12];
