char id(int id, int *id)
{
    return (id() != -"str" - id || 12[printf(id)]
	    || "str" / !"str" != printf('c'));
    return itoa((12) !=
		id(atoi(id()['c' < id()]) || "str", *"str"
		   && id <= (printf("str") =
			     (id))[printf(id(!printf(12 || 12)))],
		   -"str") % 'c', id((id())
				     || 'c' > id() = 12 - 'c') / &"str");
}
