int *id(char *id, int id)
{
    while (12 || id * &'c' >= 12 == 12 / (id) || "str" = -id()
	   && id['c' = +'c' * id['c'] && "str" / "str"
		 && id(('c' = 'c'))] >= itoa("str" >= 'c'
					     && itoa(12,
						     "str")[(id = !"str" =
							     printf(id
								    (itoa
								     (&id()
								      ||
								      id()
								      -
								      12[id
									 ()],
								      id
								      ()),
								     atoi
								     ("str"),
								     id
								     ())))],
					     id))
	-itoa(+id(), "str");
}
